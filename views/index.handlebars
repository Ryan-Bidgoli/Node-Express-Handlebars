<div class="row">

  <div class="col-sm-4">
    <ul>
      {{#each burgers}}


      {{#unless devoured}}
      burger name undevoured: <li> {{burger_name}}</li>
      <button data-id="{{id}}" id="devouredBtn">devoured</button>
      <br>
      {{/unless}}





      {{/each }}
    </ul>
  </div>
  <div class="col-sm-4">
    middle section
  </div>

  <div class="col-sm-4">

    <ul>
      {{#each burgers}}
      {{#if devoured}}

      <br>
      burger devoured: <li>{{burger_name}}</li>

      {{/if}}
      {{/each}}
    </ul>
  </div>
</div>






<input type="text" id="burgerTxt">
<button id="addBtn">add burger</button>